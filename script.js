product
function hoverProduct(element) {
    element.style.transform = 'scale(1.1)';
}

// Function to handle unhover effect on product
function unhoverProduct(element) {
    element.style.transform = 'scale(1)';
}